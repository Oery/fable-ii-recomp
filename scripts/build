#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."
mkdir -p logs
cmake --preset nix-relwithdebinfo 2>&1 | tee logs/native-configure.log
cmake --build build/native --target fable_ii -j "${BUILD_JOBS:-12}" 2>&1 | tee logs/native-build.log
