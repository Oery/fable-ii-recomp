#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."
mkdir -p logs
sha256sum -c docs/re/entry-xex.sha256
rexglue-sdk/out/linux-amd64/rexgluerd codegen fable_ii_manifest.toml "$@" 2>&1 | tee logs/codegen.log
